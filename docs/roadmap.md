# 1/3-2/3 Conjecture — Roadmap

*Generated by pm-onethird on 2026-05-08 17:00 BST. Manual edits will be overwritten on next sweep — push back via OVERRIDE mail or by editing the PM config.*

## Current state — sub-α-C arc engaged; EX-1+EX-3 done; EX-4 dispatch-stalled

**`width3_one_third_two_thirds` is hypothesis-free on `origin/main`** since 2026-05-05 (mg-8c72). Path γ floor locked (mg-bb74 `73ed85e`). Sub-α-C arc in flight for full axiom-elim; EX-1 (Stanley log-supermod) in via temp axiom (mg-d0fc Option A); EX-3 (order polytope) GREEN (mg-8c66); EX-4 (Stanley vertex theorem) Session A scoping filed but **stuck in `available` ~5.5h** awaiting mayor dispatch.

## Today's full-day arc (2026-05-08)

1. **Morning sweep 09:00 BST** — filed mg-d0fc (Option A temp axiom for Stanley log-supermod) per yesterday's pick.
2. **mg-d0fc merged AMBER** (`00cbc2d`) — `OneThird.LinearExt.stanley_log_supermod` axiomatized + `lean/AXIOMS.md` 4-condition audit-bar entry + state.md update + corollary deferred (non-blocking). Trust surface for headline unchanged; new axiom only consumed by sub-α-C arc.
3. **mg-163f filed + merged GREEN-A** (`9e6edcd`) — Path-A-vs-Path-B fork resolved. Path B's level-`k` localisation step AMBER-leaning-RED across all 4 candidate sub-formulations. Path A committed: ~4450-7700 LoC over ~12-16 polecat sessions / ~9-15 weeks calendar.
4. **mg-8c66 filed + merged GREEN** (`ed9f6e6`) — EX-3 order polytope `O(α)` as Lean type. 300-LoC `OrderPolytope.lean` with all 5 basic instances (Convex, IsClosed, Bornology.IsBounded, IsCompact, MeasurableSet) proven; canonical 3-antichain instance verified. Trust surface unchanged.
5. **mg-4831 filed** — EX-4 Session A scoping (Stanley vertex theorem proof writeup + mathlib `Set.extremePoints` API verification). 350k cap, latex-first.
6. **mg-4831 stalled on dispatch.** Mayor mailbox shows 3 unread (mg-8c66 merged refinery alert + mg-4831 dispatch + PM status ping); mayor's last read on my channel was ~09:00 BST. PM pinged at 14:30 BST; no response.

## Direction — sub-α-C critical path executing per mg-91be / mg-163f

| Ticket | Status | What |
|--------|--------|------|
| EX-1 (Stanley log-supermod) | done via temp axiom (`stanley_log_supermod`); replacement gated on DH-1 or Option B | mg-c7b9 + mg-7928 + mg-d0fc |
| EX-2 (μ log-supermod corollary) | deferred narrow follow-up; not EX-3 blocking | bundle target |
| EX-3 (order polytope) | done | mg-8c66 (`ed9f6e6`) |
| **EX-4 (Stanley vertex theorem) — Session A scoping** | **stalled on mayor dispatch** | mg-4831 |
| EX-4 Session B (Lean port) | queued | post-mg-4831 GREEN |
| EX-5 (chamber simplex + decomposition) | future | mathlib-PR-class measure theory |
| EX-6 (continuous FKG/AD on cube) | future | DH-4 leverage; mathlib-PR-class ~1000-2000 LoC |
| EX-7 (drops headline via chamber + FKG) | future | short argument once EX-6 lands |
| EX-8 (case3-port-2: drops → balanced-pair on case3) | future | ~150-300 LoC |
| EX-9 (Brightwell-port-A: drops → centred-sum) | future | ~500-800 LoC; DH-3 amortisation possibility |

**ETA contingencies** (per mg-91be §8 + mg-163f §2.2): GREEN end-to-end ~9-15 weeks calendar including mathlib upstream cycles.

## Daniel-help leverage points (unchanged from yesterday + heightened post-mg-7928)

| ID | What | Status | Cost saving |
|----|------|--------|-------------|
| **DH-1** | **Mathlib upstream Stanley log-supermod PR** (Yael Dillies + Combinatorics community) | **HIGHEST LEVERAGE** — replaces the temp axiom; ~3000-5000 LoC project work amortised to mathlib | Most actionable: mathlib Zulip outreach to Yael Dillies (maintains `Combinatorics.SetFamily.FourFunctions`) about Stanley log-supermod (Stanley 1981) as a PR. **OPT-IN ASK FOR DANIEL when convenient.** |
| DH-2 | Thin-slice for case3 application only | Speculative; assess at EX-3 / EX-5 | ~30-50% on EX-3-EX-5 if applicable |
| DH-3 | Brightwell-port-A vs -port-B amortisation | Defer to EX-9 scoping | Up to 50% on Brightwell-port |
| DH-4 | Continuous-FKG bypass | Defer to EX-6 scoping | ~1000-2000 LoC on Path A |

Per mg-7928: **DH-1 is the highest-leverage path by a wide margin** post-Bjorner-RED — a successful mathlib upstream PR collapses ~3000-5000 LoC of project work to ~50 LoC consumer + retires the temp axiom.

## Now (in flight)

- **mg-4831** *(in `available`; mayor dispatch stalled ~5.5h)* — EX-4 Session A scoping. PM pinged mayor at 14:30 BST; no response.
- **mg-344a** *(available, human)* — Daniel workspace.
- **mg-b8f9** *(available, human)* — Hopkins outreach #2. 21d post-Chan now.
- **mg-fb16** *(shelved, indefinite)* — forum publication; Daniel-scope.

## Next (queued)

- EX-4 Session B (Lean port; post-mg-4831 GREEN).
- Brightwell-port-A vs B clarification (post-EX-7).

## Later (proposed)

- EX-5, EX-6, EX-7, EX-8, EX-9 per critical path.
- DH-1 unblock event (mathlib upstream Stanley) → temp axiom retirement + ~3000-5000 LoC project work amortised away.
- Option B fallback (full Stanley AF port, ~3000-5000 LoC) if DH-1 doesn't land within ~weeks.

## Backlog (open but no near-term plan)

- mg-5f0c (shelved via η₅, 2026-04-27).
- Legacy `one-third`-tagged shelved stack (~30 items).
- 6 scoping branches.
- mg-ebff (deferred).

## Recently shipped (last 24h, side branch `a8-s2-cont-execution-arc`)

- `ed9f6e6` — mg-8c66 EX-3 OrderPolytope α + 5 basic instances.
- `9e6edcd` — mg-163f Path-A-vs-Path-B fork resolved GREEN-A.
- `00cbc2d` — mg-d0fc EX-1 Option A `stanley_log_supermod` temp axiom + AXIOMS.md entry.

## Trajectory

**Today: high-information morning, dispatch-stalled afternoon.** From 09:00-11:00 BST: temp axiom landed (Option A executing), fork resolved (GREEN-A; Path A committed), EX-3 landed cleanly. From 11:30 BST onward: mg-4831 dispatch stalled; mayor unresponsive on my channel for ~6h. PM pinged at 14:30 BST politely; awaiting response.

**Cumulative-state-doc tactic continues to validate.** state.md grew with each polecat session: mg-d0fc added §1.11, mg-163f added §1.12 + 186-line update, mg-8c66 added §1.13. Each polecat reads first + updates as part of deliverable. Pattern: every sub-α-C decision gets logged with full attribution.

**End-state** (conditional on chain landing):
- GREEN with DH-1 success: trust surface = mathlib + native_decide. ~6-9 weeks.
- GREEN without DH-1 (Option B fallback): trust surface = mathlib + native_decide. ~12-16 weeks.
- AMBER pivots: re-scoped at each step.
- RED at chamber-decomp level: PM surfaces — sub-α-C may need fundamental rethink.

**Lessons codified as feedback memory** (newest first): *Long arcs to full formalization are PM authority* (Daniel 2026-05-07). *Cumulative-state doc per polecat session* (Daniel 2026-05-06; validated 4x already across mg-21a4 / mg-91be / mg-c7b9 / mg-7928 / mg-d0fc / mg-163f / mg-8c66). *PM announces destination + ETA* (Daniel 2026-05-06). All hold.

## Decisions awaiting Daniel

PM does NOT pre-pick on these. Daniel acts when convenient.

1. **DH-1 mathlib upstream Stanley** *(highest leverage; opt-in)* — mathlib Zulip outreach to Yael Dillies (maintains `Combinatorics.SetFamily.FourFunctions`) about Stanley log-supermod upstream PR. Retires the temp axiom + amortises ~3000-5000 LoC project work to mathlib.
2. **mg-fb16 unhold** — forum publication call; project-life-level. Trust surface narrative now: 2 named (case3 + Brightwell) + 1 temp (Stanley) + 5 native_decide.
3. **mg-8487 merge to main** *(optional)* — case3-port-1 orientation lemma; standalone foundational.
4. **Hopkins outreach (mg-b8f9)** — 21d post-Chan; Daniel-action.
5. **Mayor dispatch latency** *(no Daniel action needed unless persists)* — mg-4831 stalled in `available` ~5.5h; mayor unresponsive on my channel since 09:00. PM ping sent. If persists into tomorrow morning, may indicate mayor needs restart or Daniel intervention.

PM idle until next sweep, mail traffic, or Daniel action. Trip-wire fires on any future ticket get immediate mail per `feedback_polecat_stop_runaway`.
